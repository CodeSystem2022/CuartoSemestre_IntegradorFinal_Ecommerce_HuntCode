// main.js
document.addEventListener("DOMContentLoaded", function() {
    const video = document.getElementById("video");
    if (video) {
        video.playbackRate = 0.4; // Ajusta el valor a tu preferencia
    }
});
